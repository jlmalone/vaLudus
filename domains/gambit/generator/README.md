# Gambit generator placeholder

The generator will derive development, held-out, and adversarial positions from a pinned rules
revision. It must record the source commit, generator seed, and release time, and must prevent a
candidate from obtaining held-out positions through the public benchmark package.

The first fixture plan is the [Capablanca family generator](CAPABLANCA_FAMILY.md).
