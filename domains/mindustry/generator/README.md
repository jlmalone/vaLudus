# Mindustry generator placeholder

The generator will create or select maps, waves, resource distributions, and disruption events
from a pinned game and mod revision. It must separate development from held-out seeds and preserve
the generator code and configuration needed to recreate an equivalent world.
