# Mindustry pipeline placeholder

The future pipeline will start a pinned local simulation, apply a bounded action interface, save
the initial and final world states plus the full action trace, and score declared objectives. It
must not use a live public server or present an unverified modded world as a standard-game result.
