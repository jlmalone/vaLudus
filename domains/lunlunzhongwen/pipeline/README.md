# LunLunZhongWen pipeline placeholder

The future pipeline will render a lesson from pinned source data and an explicit learner request,
retain the generated lesson specification and media checks, and score it against a declared
rubric. Human educational judgment must remain separately labeled from deterministic checks.
